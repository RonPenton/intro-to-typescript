export const e = 2.71828;

export function double(x: number): number {
    return x * 2;
}

export default interface Fraction {
    numerator: number,
    denominator: number
}

export let x = {
    value: 42
}